#include<bits/stdc++.h>
using namespace std;

int subXORbtr(vector<int> &arr, int n){
    
}

int main(){
    int n;
    cin >> n;
    vector<int> arr(n, 0);
    for (int i = 0; i < arr.size(); i++)
    {
        cin >> arr[i];
    }
    
    int ans = subXORbtr(arr, k);
    cout << ans << " ";
}