#include<bits/stdc++.h>
using namespace std;


int inversionCount(vector<int>& arr) {

        
}

int main(){
    int n;
    cin >> n;
    vector<int> arr(n, 0);
    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }
    
    int ans = inversionCount(arr);
}