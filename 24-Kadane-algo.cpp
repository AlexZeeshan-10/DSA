#include <bits/stdc++.h>
using namespace std;


//Optimal approach for Max sum of subarray
int Kadane_algo(vector<int> &arr){
    
}

int main(){

    int n;
    cin >> n;
    vector<int> arr(n);
    for (int i = 0; i < arr.size(); i++){
        cin >> arr[i];
    }
    

}